name: Tests

on:
  pull_request:
  push:

defaults:
  run:
    shell: 'bash -Eeuo pipefail -x {0}'

jobs:
  deb822:
    name: deb822-json
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: ./tests/deb822.sh
